{
  "name": "codex-window-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A containerized web service that schedules messages into a persistent Codex CLI app-server thread.",
  "scripts": {
    "start": "node src/server.mjs",
    "check": "node --check src/server.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {}
}
